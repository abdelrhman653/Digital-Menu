rules_version = '2';

// Firebase Storage is intentionally unused by this project.
// Keep Storage closed so no accidental uploads are possible.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
